const chai = require('chai');
const chaiHttp = require('chai-http');
const app = require('../app');
const expect = chai.expect;
const {Category} = require('../server/models');

describe('Resource Model', () => {
    describe('Add a resoruce to the database', () => {

    });
});